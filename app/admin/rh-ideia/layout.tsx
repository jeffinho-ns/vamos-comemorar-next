import type { ReactNode } from "react";

/** Layout admin Ideia RH — gate herdado de app/admin/layout.tsx */
export default function RhIdeiaAdminLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
